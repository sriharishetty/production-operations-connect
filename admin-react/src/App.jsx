import React, { useEffect, useMemo, useState } from "react";
import LoginScreen from "./Login.jsx";
import VendorEditor from "./VendorEditor.jsx";

const STORAGE = { authenticated: "productionOperationsAdminAuthenticated", email: "productionOperationsAdminEmail", password: "productionOperationsAdminPassword", vendors: "productionOperationsVendors", applications: "productionOperationsApplications", history: "productionOperationsAdminHistory" };
const ADMINS = ["Srihari.Gangisetti@alaskaair.com", "Mohan.P@alaskaair.com", "Margaret.Jennifer@alaskaair.com", "Sharmini.M@alaskaair.com", "Srinivasanagasai.Chevitikanti@alaskaair.com"];
const fields = ["systemName", "category", "vendorCompanyName", "prodOpsEscalationProcess", "contactNumbers", "emailAddress", "vendorPOC", "aagItsPOC", "aagItsTeam", "infoUpdatedDate", "infoUpdatedBy", "infoApprovedDate", "infoApprovedBy"];
const read = (key, fallback) => { try { const value = JSON.parse(localStorage.getItem(key)); return Array.isArray(value) ? value : fallback; } catch { return fallback; } };
const workbookVendors = () => {
  if (!Array.isArray(window.masterData?.vendors)) return [];
  const fieldsToMerge = ["category", "vendorCompanyName", "prodOpsEscalationProcess", "contactNumbers", "emailAddress", "vendorPOC", "aagItsPOC", "aagItsTeam", "infoUpdatedDate", "infoUpdatedBy", "infoApprovedDate", "infoApprovedBy"];
  const records = [];
  window.masterData.vendors.forEach(vendor => {
    const mapped = { id: `its-application-vendors-${vendor.sourceRow}`, sourceRow: vendor.sourceRow, vendorDataVersion: 2, systemName: vendor.systemName || "", category: vendor.category || "", vendorCompanyName: vendor.vendor || "", prodOpsEscalationProcess: vendor.escalation || "", contactNumbers: vendor.phone || "", emailAddress: vendor.email || "", vendorPOC: vendor.vendorPoc || "", aagItsPOC: vendor.aagItsPoc || "", aagItsTeam: vendor.aagItsTeam || "", infoUpdatedDate: vendor.updatedDate || "", infoUpdatedBy: vendor.updatedBy || "", infoApprovedDate: vendor.approvedDate || "", infoApprovedBy: vendor.approvedBy || "" };
    if (mapped.systemName) { records.push(mapped); return; }
    const parent = records.at(-1);
    if (!parent) return;
    fieldsToMerge.forEach(field => { if (mapped[field]) parent[field] = [parent[field], mapped[field]].filter(Boolean).join("\n\n"); });
  });
  return records;
};

function Login({ onLogin }) {
  const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [message, setMessage] = useState(""); const [showPassword, setShowPassword] = useState(false); const [grantingAccess, setGrantingAccess] = useState(false);
  function submit(event) { event.preventDefault(); if (!ADMINS.includes(email) || password !== (localStorage.getItem(STORAGE.password) || "123456789")) return setMessage("Invalid administrator email or password."); setMessage(""); setGrantingAccess(true); localStorage.setItem(STORAGE.authenticated, "true"); localStorage.setItem(STORAGE.email, email); window.setTimeout(() => onLogin(email), 850); }
  return <main className="login"><form onSubmit={submit}><div className="login-brand"><strong>Alaska.</strong><span>AIRLINES</span><b>🔐</b><h1>Admin Control Center</h1><p>Authorized administrators only</p></div><label>Administrator Email<div className="input-wrap"><i>✉</i><input type="email" placeholder="name@alaskaair.com" value={email} onChange={e => setEmail(e.target.value)} required /></div></label><label>Password<div className="input-wrap"><i>🔑</i><input type={showPassword ? "text" : "password"} placeholder="Enter password" value={password} onChange={e => setPassword(e.target.value)} required /><button className="show-password" type="button" onClick={() => setShowPassword(value => !value)}>{showPassword ? "Hide" : "Show"}</button></div></label>{message && <p className="error">{message}</p>}<button className="sign-in">Sign In →</button><div className="security-note"><span>🔒</span> Administrative access is restricted to authorized Production Operations members.</div></form></main>;
}

function Editor({ vendor, onClose, onSave }) {
  const [draft, setDraft] = useState(vendor);
  return <div className="modal"><form className="editor" onSubmit={e => { e.preventDefault(); onSave(draft); }}><header><div><small>EDIT</small><h2>Edit Vendor Information</h2></div><button type="button" onClick={onClose}>×</button></header><div className="form-grid">{fields.map(field => <label key={field}>{field.replace(/([A-Z])/g, " $1")} {field === "escalation" || field === "supportNotes" || field === "documents" ? <textarea value={draft[field] || ""} onChange={e => setDraft({ ...draft, [field]: e.target.value })} /> : <input type={field.includes("Date") ? "date" : "text"} value={draft[field] || ""} onChange={e => setDraft({ ...draft, [field]: e.target.value })} />}</label>)}</div><footer><button type="button" className="secondary" onClick={onClose}>Cancel</button><button>Save Changes</button></footer></form></div>;
}

function ApplicationEditor({ application, onClose, onSave }) {
  const [draft, setDraft] = useState(application);
  const appFields = ["name", "description", "vendor", "severity", "status", "icon"];
  return <div className="modal"><form className="editor" onSubmit={e => { e.preventDefault(); onSave(draft); }}><header><div><small>EDIT</small><h2>Edit Application Information</h2></div><button type="button" onClick={onClose}>×</button></header><div className="form-grid">{appFields.map(field => <label key={field}>{field}<input value={draft[field] || ""} onChange={e => setDraft({ ...draft, [field]: e.target.value })} /></label>)}</div><footer><button type="button" className="secondary" onClick={onClose}>Cancel</button><button>Save Changes</button></footer></form></div>;
}

function PasswordEditor({ onClose }) {
  const [current, setCurrent] = useState(""); const [next, setNext] = useState(""); const [confirm, setConfirm] = useState(""); const [message, setMessage] = useState("");
  function save(event) { event.preventDefault(); if (current !== (localStorage.getItem(STORAGE.password) || "123456789")) return setMessage("Current password is incorrect."); if (next.length < 8) return setMessage("New password must contain at least 8 characters."); if (next !== confirm) return setMessage("New passwords do not match."); localStorage.setItem(STORAGE.password, next); onClose(); }
  return <div className="modal"><form className="editor password-editor" onSubmit={save}><header><div><small>SECURITY</small><h2>Change Password</h2></div><button type="button" onClick={onClose}>×</button></header><label>Current Password<input type="password" value={current} onChange={e => setCurrent(e.target.value)} required /></label><label>New Password<input type="password" value={next} onChange={e => setNext(e.target.value)} required /></label><label>Confirm New Password<input type="password" value={confirm} onChange={e => setConfirm(e.target.value)} required /></label>{message && <p className="error">{message}</p>}<footer><button type="button" className="secondary" onClick={onClose}>Cancel</button><button>Update Password</button></footer></form></div>;
}

export default function App() {
  const [email, setEmail] = useState(() => localStorage.getItem(STORAGE.email));
  const [vendors, setVendors] = useState(() => { const saved = read(STORAGE.vendors, []); const source = workbookVendors(); if (saved.length && saved.every(vendor => vendor.vendorDataVersion === 2)) return saved; return source.map(record => { const prior = saved.find(vendor => vendor.sourceRow === record.sourceRow); if (!prior || !Array.isArray(prior.editedFields)) return record; return prior.editedFields.reduce((merged, field) => fields.includes(field) ? { ...merged, [field]: prior[field] || "" } : merged, record); }); });
  const [applications, setApplications] = useState(() => read(STORAGE.applications, Object.values(window.masterData?.applications || {})));
  const [history, setHistory] = useState(() => read(STORAGE.history, [])); const [query, setQuery] = useState(""); const [editing, setEditing] = useState(null); const [editingApplication, setEditingApplication] = useState(null); const [changingPassword, setChangingPassword] = useState(false);
  useEffect(() => { const logout = () => { localStorage.removeItem(STORAGE.authenticated); localStorage.removeItem(STORAGE.email); }; addEventListener("pagehide", logout); return () => removeEventListener("pagehide", logout); }, []);
  useEffect(() => localStorage.setItem(STORAGE.vendors, JSON.stringify(vendors.map(v => { const base = workbookVendors().find(x => x.sourceRow === v.sourceRow) || {}; return { ...v, editedFields: fields.filter(key => (v[key] || "") !== (base[key] || "")) }; }))), [vendors]);
  useEffect(() => localStorage.setItem(STORAGE.applications, JSON.stringify(applications)), [applications]);
  const results = useMemo(() => vendors.filter(v => Object.values(v).join(" ").toLowerCase().includes(query.toLowerCase())), [vendors, query]);
  const profileName = (email || "Administrator").split("@")[0].split(".").map(part => part ? `${part[0].toUpperCase()}${part.slice(1)}` : "").filter(Boolean).join(" ");
  const profileInitials = profileName.split(" ").map(part => part[0]).join("").slice(0, 2).toUpperCase() || "AD";
  const currentPath = window.location.pathname.replace(/\\/g, "/").replace(/\/$/, "").toLowerCase();
  const isActiveNavigation = href => new URL(href, window.location.href).pathname.replace(/\\/g, "/").replace(/\/$/, "").toLowerCase() === currentPath;
  function saveVendor(draft) { setVendors(items => items.map(v => v.id === draft.id ? draft : v)); const item = { action: "Vendor updated", details: `${draft.vendorCompanyName || draft.systemName || "Vendor"} information was updated`, timestamp: new Date().toLocaleString() }; const next = [item, ...history].slice(0, 50); setHistory(next); localStorage.setItem(STORAGE.history, JSON.stringify(next)); setEditing(null); }
  function saveApplication(draft) { setApplications(items => items.map(app => app.id === draft.id ? draft : app)); const item = { action: "Application updated", details: `${draft.name || "Application"} information was updated`, timestamp: new Date().toLocaleString() }; const next = [item, ...history].slice(0, 50); setHistory(next); localStorage.setItem(STORAGE.history, JSON.stringify(next)); setEditingApplication(null); }
  if (!email || localStorage.getItem(STORAGE.authenticated) !== "true") return <LoginScreen onLogin={setEmail} />;
  const navigation = [["Home", "../home.html", "\u2302"], ["Vendors", "../vendors.html", "\u265f"], ["Categories", "../categories.html", "\u25a6"], ["Ground Stop Apps", "../home.html#ground-stop-apps", "\u2708"], ["Favorites", "../favorites.html", "\u2606"], ["Reports", "../reports.html", "\u25a4"]];
  return <main className="app"><aside className="sidebar"><div className="sidebar-brand"><img className="sidebar-brand-icon" src="../public/sidebar-logo.png" alt="Alaska Airlines logo" /><div><h1>Alaska.</h1><p>AIRLINES</p></div></div><nav className="sidebar-nav">{navigation.map(([label, href, icon]) => <a key={label} href={href} className={`nav-item${isActiveNavigation(href) ? " active" : ""}`}><span className="nav-icon" aria-hidden="true">{icon}</span><span>{label}</span></a>)}<a className={`nav-item${isActiveNavigation("./index.html") ? " active" : ""}`} href="./index.html"><span className="nav-icon" aria-hidden="true">{"\ud83d\udee1"}</span><span>Admin</span></a></nav><div className="sidebar-bottom"><div className="sidebar-quote">People.<br />Planes.<br />A Brighter<br />Tomorrow.</div><div className="sidebar-copyright">© 2026 Alaska Airlines<br />All rights reserved.</div></div></aside><section><header className="top"><div>  <small>ITS Production Operations</small><h1>Admin Control Center</h1><p>Manage applications, vendors and operational information.</p></div><div className="top-right"><div className="admin-profile-card" title={email}><div className="admin-profile-avatar">{profileInitials}</div><div className="admin-profile-info"><strong>Welcome,</strong><span>{profileName}</span><small>Production Operations</small></div></div><div className="top-actions"><button className="secondary" onClick={() => setChangingPassword(true)}>Change Password</button><button className="secondary" onClick={() => { localStorage.removeItem(STORAGE.authenticated); localStorage.removeItem(STORAGE.email); setEmail(null); }}>Logout</button></div></div></header><div className="stats"><article><strong>{applications.length}</strong><span>Applications</span><i className="stat-icon" aria-hidden="true">✈</i></article><article><strong>{vendors.length}</strong><span>Vendor records</span><i className="stat-icon" aria-hidden="true">♟</i></article><article><strong>{history.length}</strong><span>Changes</span><i className="stat-icon" aria-hidden="true">↻</i></article></div><article className="panel"><header><div><h2>Application Management</h2><p>Edit all application information.</p></div></header><div className="list">{applications.map(app => <div className="row" key={app.id}><div><strong>{app.name}</strong><span>{app.description}</span></div><button onClick={() => setEditingApplication(app)}>Edit</button></div>)}</div></article><article className="panel"><header><div><h2>Vendor Management</h2><p>Update vendor information. Changes sync to the Vendor Directory.</p></div><input placeholder="Search vendors..." value={query} onChange={e => setQuery(e.target.value)} /></header><div className="list">{results.map(v => <div className="row" key={v.id}><div><strong>{v.vendorCompanyName || v.systemName || "Vendor"}</strong><span>{v.systemName || v.category || "Vendor information"}</span></div><button onClick={() => setEditing(v)}>Edit</button></div>)}</div></article><article className="panel"><h2>Recent Changes</h2>{history.length ? history.slice(0, 5).map((item, index) => <p key={index}>{item.action}: {item.details}</p>) : <p>No changes yet.</p>}</article></section>{editing && <VendorEditor vendor={editing} onClose={() => setEditing(null)} onSave={saveVendor} />}{editingApplication && <ApplicationEditor application={editingApplication} onClose={() => setEditingApplication(null)} onSave={saveApplication} />}{changingPassword && <PasswordEditor onClose={() => setChangingPassword(false)} />}</main>;
}
